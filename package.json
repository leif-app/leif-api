{
  "scripts": {
    "dev": "wrangler dev --minify src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "@flatten-js/interval-tree": "^1.0.20",
    "date-fns": "^2.30.0",
    "entsoe-api-client": "^1.0.0",
    "hono": "^3.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "wrangler": "^3.0.0"
  }
}
